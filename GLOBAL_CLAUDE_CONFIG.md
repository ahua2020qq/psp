# Claude Code 全局配置完成指南

> 配置日期：2026-01-14
> 配置位置：`C:/Users/ahua/.claude/`

## ✅ 已完成的配置

### 1. 全局设置文件

**路径**: `~/.claude/settings.json`

**配置内容**:

```json
{
  "env": {
    "ANTHROPIC_AUTH_TOKEN": "...",
    "ANTHROPIC_BASE_URL": "https://open.bigmodel.cn/api/anthropic",
    "API_TIMEOUT_MS": "3000000",
    "CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC": 1
  },
  "language": "chinese",                    // ✅ 中文界面
  "attribution": {                          // ✅ 提交归属
    "commit": "Co-Authored-By: Claude...",
    "pr": "Generated with Claude Code..."
  },
  "respectGitignore": true,                 // ✅ 尊重 .gitignore
  "cleanupPeriodDays": 90,                  // ✅ 90天清理历史
  "permissions": {                          // ✅ 权限设置
    "defaultMode": "acceptEdits",           // 默认接受编辑
    "ask": [                                // 危险操作需确认
      "allowDeletingFile",
      "allowUnsandboxedCommand"
    ]
  },
  "spinnerTipsEnabled": true,               // ✅ 显示加载提示
  "promptSuggestionEnabled": true,          // ✅ 启用建议
  "syntaxHighlightingDisabled": false       // ✅ 语法高亮
}
```

### 2. 全局 Skills

**路径**: `~/.claude/skills/`

**已创建的 Skills**:

| Skill | 功能 | 参数 |
|-------|------|------|
| `smart-commit` | 智能提交代码 | message (可选) |
| `review` | 代码审查 | 无 |
| `test` | 运行测试 | 无 |
| `explain` | 解释代码 | target (必需) |
| `build` | 构建项目 | environment (可选) |
| `clean` | 清理项目 | 无 |

### 3. 项目特定配置（PSP 项目）

**路径**: `D:\zhipu\psp\.claude\`

**已配置**:
- ✅ Slash Commands（6个）
- ✅ 项目特定 Skills（3个）
- ✅ Hooks 配置

---

## 🎯 配置优先级和冲突处理

### 配置加载顺序

```
1. 全局配置 (~/.claude/settings.json)
   ↓
2. 项目配置 (项目/.claude/settings.json)
   ↓
3. 合并配置（项目配置优先）
```

### 冲突处理规则

| 配置类型 | 全局 | 项目 | 优先级 |
|---------|------|------|--------|
| **环境变量** | ✅ | ❌ | 全局 |
| **语言偏好** | ✅ | ❌ | 全局 |
| **权限设置** | ✅ | ✅ | 项目覆盖 |
| **Slash Commands** | ❌ | ✅ | 仅项目 |
| **Skills** | ✅ | ✅ | **共存** ✅ |
| **Hooks** | ✅ | ✅ | **合并执行** ✅ |

### 重要说明

#### ✅ Skills 可以共存

```
全局 Skills (~/.claude/skills/)
  ├─ smart-commit    ✅ 所有项目可用
  ├─ review          ✅ 所有项目可用
  ├─ test            ✅ 所有项目可用
  └─ ...

项目 Skills (项目/.claude/skills/)
  ├─ cf-deploy       ✅ 仅 PSP 项目可用
  ├─ analyze-perf    ✅ 仅 PSP 项目可用
  └─ ...

最终效果：
- 在 PSP 项目中：全局 Skills + 项目 Skills ✅
- 在其他项目中：只有全局 Skills ✅
```

#### ✅ Hooks 会合并执行

```
全局 Hooks (~/.claude/settings.json)
  └─ SessionStart: 记录启动时间

项目 Hooks (项目/.claude/settings.json)
  └─ PreToolUse: 记录工具使用

最终效果：
- 全局 Hooks 先执行
- 项目 Hooks 后执行
- 两者都生效 ✅
```

---

## 📋 可用的命令和 Skills

### 全局可用（所有项目）

```
Skills:
/smart-commit      - 智能提交代码
/review            - 代码审查
/test              - 运行测试
/explain target    - 解释代码
/build [env]       - 构建项目
/clean             - 清理项目
```

### PSP 项目专属

```
Slash Commands:
/commit            - 智能提交（项目定制版）
/deploy            - Cloudflare 部署
/test              - 运行测试
/clean             - 清理缓存
/review            - 代码审查
/cache-stats       - 缓存统计

Skills:
/cf-deploy         - CF 部署（详细版）
/analyze-perf      - 性能分析
```

---

## 🚀 使用示例

### 在任何项目中（使用全局配置）

```powershell
cd any-project
claude

你：/smart-commit "添加新功能"

我：✅ 自动执行全局提交流程...
```

### 在 PSP 项目中（全局 + 项目配置）

```powershell
cd D:\zhipu\psp
claude

你：/cf-deploy production

我：✅ 执行项目专属的部署流程...
   （使用 Cloudflare 配置）

你：/review

我：✅ 执行全局代码审查...
   （可用的全局 Skill）
```

---

## 🔧 配置对比

### 之前的配置（仅项目）

```
D:\zhipu\psp\
└── .claude/
    ├── settings.json       ✅ 项目配置
    └── skills/
        ├── cf-deploy.md    ✅ 仅 PSP 可用
        └── analyze-perf.md ✅ 仅 PSP 可用

问题：
❌ 其他项目无法复用配置
❌ 每个项目都要重新配置
❌ 通用命令不统一
```

### 现在的配置（全局 + 项目）

```
~/.claude/                    # ✅ 全局配置
├── settings.json            # 全局设置（语言、权限等）
└── skills/                  # ✅ 全局 Skills
    ├── smart-commit.md      # 所有项目可用
    ├── review.md            # 所有项目可用
    ├── test.md              # 所有项目可用
    ├── explain.md           # 所有项目可用
    ├── build.md             # 所有项目可用
    └── clean.md             # 所有项目可用

D:\zhipu\psp\.claude/        # ✅ 项目配置
├── settings.json            # 项目 Slash Commands
└── skills/                  # ✅ 项目特定 Skills
    ├── cf-deploy.md         # 仅 PSP 可用
    └── analyze-perf.md      # 仅 PSP 可用

优势：
✅ 全局 Skills 所有项目可用
✅ 项目特定配置不影响其他项目
✅ 配置清晰分离
✅ 易于维护和扩展
```

---

## 💡 最佳实践

### 1. 配置分类原则

```
┌─────────────────────────────────────┐
│  全局配置 (~/.claude/)              │
│  ┌─────────────────────────────┐   │
│  │ ✅ 通用 Skills               │   │
│  │  - commit, review, test     │   │
│  │  - explain, build, clean    │   │
│  ├─────────────────────────────┤   │
│  │ ✅ 全局设置                  │   │
│  │  - 语言偏好                  │   │
│  │  - 权限默认值                │   │
│  │  - 提交归属                  │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  项目配置 (项目/.claude/)           │
│  ┌─────────────────────────────┐   │
│  │ ✅ 项目特定 Skills           │   │
│  │  - cf-deploy               │   │
│  │  - analyze-perf            │   │
│  ├─────────────────────────────┤   │
│  │ ✅ Slash Commands           │   │
│  │  - 项目特定快捷命令          │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

### 2. 日常开发流程

```
早上启动：
1. cd 项目目录
2. claude
3. ✅ 全局配置自动加载
4. ✅ 项目配置自动加载
5. ✅ 所有 Skills 和命令可用

日常开发：
- 使用全局 /commit 提交代码
- 使用全局 /review 审查代码
- 使用项目 /cf-deploy 部署
- 使用项目 /analyze-perf 分析性能
```

### 3. 新项目配置

```
创建新项目时：
1. 全局配置自动生效 ✅
2. 只需配置项目特定内容 ✅
   - 项目特定 Skills
   - Slash Commands（可选）
3. 无需重复配置通用功能 ✅
```

---

## 📊 配置对比表

| 特性 | 之前 | 现在 |
|------|------|------|
| **通用命令** | ❌ 仅 PSP 项目 | ✅ 所有项目 |
| **提交流程** | ❌ 每项目配置 | ✅ 全局统一 |
| **代码审查** | ❌ 每项目配置 | ✅ 全局统一 |
| **测试运行** | ❌ 每项目配置 | ✅ 全局统一 |
| **项目特定** | ✅ PSP 专属 | ✅ 仍保留 |
| **配置维护** | ❌ 重复配置 | ✅ 一次配置，到处使用 |
| **配置冲突** | ❌ 不适用 | ✅ 智能合并，无冲突 |

---

## ✅ 总结

### 配置完成情况

- ✅ **全局配置**：~/.claude/settings.json
- ✅ **全局 Skills**：6 个通用 Skills
- ✅ **项目配置**：PSP 项目特定配置保持不变
- ✅ **无冲突**：全局和项目配置完美共存

### 下一步

1. **重启 Claude Code**（让配置生效）
   ```powershell
   exit
   claude
   ```

2. **测试全局 Skills**
   ```
   /smart-commit
   /review
   /test
   ```

3. **享受效率提升**
   - 所有项目都能使用通用 Skills
   - PSP 项目仍有专属功能
   - 配置清晰，维护简单

---

**最后更新**: 2026-01-14
**维护者**: 姝姝豆芽之家
